html do
  head do
		title '10to1 - Timesheets made easy' 
		stylesheet_link_tag 'styles', :media => 'screen'
		stylesheet_link_tag 'print', :media => 'print'
  end

  body do
    # p flash[:notice], :style => "color: green"
    self << content_for_layout
  end
end
